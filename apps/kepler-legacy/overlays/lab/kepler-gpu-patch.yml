apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kepler
  namespace: kepler
spec:
  template:
    spec:
      runtimeClassName: "nvidia"
      containers:
      - args:
        - -v=$(KEPLER_LOG_LEVEL)
        - -enable-gpu
